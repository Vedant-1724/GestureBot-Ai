# Install: pip install -r requirements.txt
# PyTorch (CUDA): pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

ultralytics>=8.3.0          # YOLO11 classification
opencv-python>=4.10.0       # Video stream + frame display
Pillow>=10.3.0              # Image I/O and preprocessing
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.5.0         # Classification report, ROC, PR curves
scipy>=1.13.0
matplotlib>=3.9.0
seaborn>=0.13.0
requests>=2.32.0            # MJPEG stream from ESP32-CAM
tqdm>=4.66.0                # Progress bars
split-folders>=0.5.1        # Train/val/test splitting
PyYAML>=6.0.1               # YAML config for YOLO
kaggle>=1.6.0               # Optional dataset download
